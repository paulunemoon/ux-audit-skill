# macOS
.DS_Store

# Editor / local tooling state
.coplex/
.vscode/
.idea/
.claude/settings.local.json

# Reports generated by the skill while dogfooding it on this repo.
# Anchored and prefixed on purpose — a bare `audit.md` also matches
# .claude/commands/audit.md, which is part of the skill.
/audit.md
/ux-audit-*.md

# Python
__pycache__/
*.py[cod]
